name: Publish NuGet Package

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  publish:
    uses: Resurrection-Capsule/.github/.github/workflows/dotnet-nuget.yml@v1
    with:
      project: src/Core/AssetData.Parser.csproj
    secrets: inherit
